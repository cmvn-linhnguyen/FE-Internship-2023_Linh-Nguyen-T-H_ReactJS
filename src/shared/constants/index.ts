export enum Status {
  AVAILABLE = 'available',
  OUT_OF_STOCK = 'out_of_stock',
}

export enum CartAction {
  INCREASE = 'increase',
  DECREASE = 'decrease',
}

export enum StorageKey {
  CART = 'cart',
}

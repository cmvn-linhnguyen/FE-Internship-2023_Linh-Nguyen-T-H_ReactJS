export enum Status {
  AVAILABLE = 'available',
  OUT_OF_STOCK = 'out_of_stock',
}

export enum StorageKeys {
  CART = 'cart',
  PRODUCTS = 'products',
  USER = 'user',
}

export enum ToastTypes {
  SUCCESS = 'success',
  FAILED = 'failed',
}
